{
	"// why": "src/faq/*.test.ts run under vitest on node and read the built bundle with node:fs; the Worker tsconfig has no node types and excludes them. `npm run typecheck` runs all three configs.",
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"types": ["@cloudflare/workers-types", "node", "vitest/globals"]
	},
	"include": ["src/faq/*.test.ts"],
	"exclude": ["node_modules", "dist", ".wrangler", "site"]
}
